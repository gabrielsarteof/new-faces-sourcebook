---
/**
 * Alternador de tema.
 *
 * Espelha o contrato do criador: chave `nf_theme`, valores light | dark | system,
 * classe `dark` no elemento raiz. O card de ícone segue NavIconButton.tsx, que é
 * w-9 h-9 rounded-xl com o ícone em 22 e stroke 1.5.
 */
---
<button
  id="theme-toggle"
  type="button"
  class="app-icon-card text-mono-sub hover:text-mono-text cursor-pointer"
  aria-label="Alternar tema"
>
  <svg class="dark:hidden" width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" aria-hidden="true">
    <circle cx="12" cy="12" r="4" /><path d="M12 2v2M12 20v2M4.9 4.9l1.4 1.4M17.7 17.7l1.4 1.4M2 12h2M20 12h2M4.9 19.1l1.4-1.4M17.7 6.3l1.4-1.4" />
  </svg>
  <svg class="hidden dark:block" width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
    <path d="M21 12.8A9 9 0 1 1 11.2 3a7 7 0 0 0 9.8 9.8Z" />
  </svg>
</button>

<script>
  const KEY = 'nf_theme';
  type UserTheme = 'light' | 'dark' | 'system';

  const prefersDark = () =>
    window.matchMedia != null && window.matchMedia('(prefers-color-scheme: dark)').matches;

  const read = (): UserTheme => {
    try {
      const v = localStorage.getItem(KEY);
      return v === 'light' || v === 'dark' || v === 'system' ? v : 'system';
    } catch {
      return 'system';
    }
  };

  const resolve = (t: UserTheme) => (t === 'system' ? (prefersDark() ? 'dark' : 'light') : t);
  const apply = (t: UserTheme) =>
    document.documentElement.classList.toggle('dark', resolve(t) === 'dark');

  document.getElementById('theme-toggle')?.addEventListener('click', () => {
    const next: UserTheme = resolve(read()) === 'dark' ? 'light' : 'dark';
    try { localStorage.setItem(KEY, next); } catch { /* modo privado */ }
    apply(next);
  });

  window.matchMedia?.('(prefers-color-scheme: dark)').addEventListener('change', () => {
    if (read() === 'system') apply('system');
  });
</script>
